# Core packages
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.66.0

# Optional but recommended
scikit-learn>=1.3.0          # 如果后续用到 PCA 或其他 ML 功能
matplotlib>=3.8.0            # 如果需要画图或分析结果
seaborn>=0.13.0              # 进一步的可视化支持
